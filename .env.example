# VITE_API_URL: Base URL for the backend API. Optional — defaults to "/api",
# which is proxied by Vite's dev server to the Express server (see vite.config.ts).
# Only set this if the API is hosted on a different origin than the frontend
# (e.g. in production, where the frontend and backend are deployed separately).
VITE_API_URL=
